**After Clone Repository**
`npm install`
`npm start`
`npm run android`

**Credential for login**
username `vikas`
password `12345`

**Screens**
1. Login Screen
2. Home Screen

**Libraries which I used in my app**

1. For Camera access`react-native-vision-camera` && `react-native-image-picker`
2. For Location access `react-native-geolocation-service` , `react-native-android-location-enabler`
3. For Geo location `@react-native-community/geolocation`
4. For Permission `react-native-permissions`
